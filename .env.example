PORT=3000
DB_PATH=./data/agenter.db

# Required when using a remote OpenAI-compatible provider (e.g. Tooken Club).
# Leave blank for local-only mode with no remote API key:
#   TOOKEN_CLUB_API_KEY=
TOOKEN_CLUB_API_KEY=
